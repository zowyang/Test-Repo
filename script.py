

import requests


r = requests.get('https://google.com')
print(r.status_code)


# command + shift + P to search
# ^(control) + space to fill/view options in code
# ^(control) + option to run code
# cannot run above code runner (b.c. read only) for inputs. But can run in vscode by right clicking and clicking 'Run Python File in Terminal'
